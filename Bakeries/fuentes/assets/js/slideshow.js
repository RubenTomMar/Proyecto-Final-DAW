/**
 * Archivo deonde creamos un slideshow con jquery y fancybox ()
 */
$(document).ready(function () {//nos aseguramos de que haya cargado la página

    //usamos la libreria fancybox y creamos slideshow
    Fancybox.bind('[data-fancybox]', {

    });
});